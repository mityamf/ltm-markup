extends ../base/layout

block variables
  - var title = "Messages"
  - var hasHeader = true
  - var hasFooter = true
  - var currentPage = "MessagesRead"

block main
  include ../data/lk/lk-admin-headline
  include ../data/lk/lk-participants
  include ../data/lk/lk-admin-tabs
  include ../data/lk/lk-mail-control
  include ../data/lk/lk-messages-read
  include ../data/lk/lk-support-contacts

  main.container.container--admin-deadlines.lk
    .admin
      +renderLKAdminHeadline(adminHeadline.eventName, adminHeadline.eventPlace, adminHeadline.dates)
      +renderLKParticipantList(participantsHeadline, participantsInfo, participantsList, participantsText, infoBlock)
      .admin__control-wrapper
        +renderAdminTabs(adminTabs)
        +renderMailControl("Сообщение", adminMailControl.linkList, adminMailControl.isSearchInput)

      h2.admin-table__headline="Сообщение"
      +renderEditBlock(editingBlockList, buttons, editingBlockStatus)
    +renderSupportContacts(contactsList)
