mixin renderAlphabetHotel(title, hotelList, link)
  .alphabet-hotel__block-item
    h3.alphabet-hotel__block-title=title
    ul.alphabet-hotel__hotels
      each hotel in hotelList
        li
          a(href=hotel.link)=hotel.name
